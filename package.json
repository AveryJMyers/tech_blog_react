{
  "name": "tech_blog_react",
  "version": "1.0.0",
  "description": "tech blog handlebars --> React",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "seed": "cd server && npm run seeds",
    "build": "cd client && npm run build"
  },
  "author": "Avery Myers",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.1",
    "sequelize": "^6.32.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
